# M5b-IF CODE/CONSISTENCY-AXIS REVIEW — GwzM5-8M5bNoFfDesign.md (DRAFT, 783 lines)

Reviewer: F5, Code/Consistency axis, independent and read-only. State axis reviewed separately; nothing here relies on it. Every code citation below was re-derived from the tree, not taken from the design. Filed verbatim by the lane owner.

## 0. Review basis

- Review object: `dev-docs/GwzM5-8M5bNoFfDesign.md` — **uncommitted** (untracked) in gwz-dev at `0298edb` ("Adopt the A1 decision packet's three recommendations").
- Code tree: gwz-core HEAD `43c37bcff338daf95f053eedbc7467e6c9368eff`. The design cites the checkpoint tuple `f36d20d…`; the delta `f36d20d → 43c37bc` is exactly one file, `.github/workflows/platform-matrix.yml` (+43) — **no src change**, so all code citations were checkable and were checked at HEAD.
- Requirements: `GwzM5-8Refactor.md` §M5b bullets verified verbatim at 2277-2287, framing at 2288-2289; §6 at 293-295/374-376; §16.1 at 1864-1872; §22.4 item 4 at 3181-3182; A1 §"activate --no-ff only through v1" confirmed in the A1 section.

## 1. INVENTORY FIDELITY (§2) — 17 exists-claims spot-verified, 16 exact

All of the following §2.1 rows exist at the cited locations and do what the design says (file paths under `gwz-core/src/`):

1. Shared closed `MergeExecutionMode::{Normal,FfOnly,NoFf}`, snake_case, omitted-when-normal, same YAML path both versions — `workspace_ops/merge/model/lifecycle.rs:5-28`, `model/v0.rs:44-45`, `model/v1/record.rs:29-30`. Exact.
2. v0 start rejection of `--no-ff` before record creation, `MergePhaseUnsupported`, exact message — `validate.rs:12-14`; pinning test `custom_messages_validate_while_no_ff_remains_reserved` with the NoFf assertion at `validate.rs:218-225`. Exact (test body runs 201-239).
3. Typed action vocabulary `PendingMergeAction`/`PendingCommitSpec`/`PendingGitSignature{name,email,time_seconds,timezone_offset_minutes}` all with `extensions` — `model/v0.rs:98-148` (signature at 131-139). Exact — the timestamp **is** wire-frozen today, as W1 assumes.
4. Frozen §7 matrix incl. both no-ff rows and the two-parent sentence — `GwzM5-8I2RecordContract.md:324-344`. Exact.
5. Executable v1 matrix validator; `NoFf => !FastForward` kind; spec canonicality; signature bounds (±1440, `\0\n\r`, non-empty) — `model/v1/validate/action.rs:41-116` (mode arm 62-68, signature 103-109), tests `action_tests.rs:44+` (`v1_action_matrix_accepts_exact_normal_and_no_ff_commit_intents`). Exact.
6. `GitPreparedMergeMode::{AllowFastForward,ForceMergeCommit}`; ForceMergeCommit-over-ff-able prepares Commit with **source commit's tree**, signatures captured at preparation — `git/gitbackend/contract.rs:140-147`; `merge_prepared.rs:140-195` (ff arm 168-180, true-merge arm 181-193, tree ODB write 186). Exact.
7. Closed revalidation: frozen Commit over ff-able accepted **only** when frozen tree == source tree; all class drift typed `MergeRecoveryRequired` — `merge_support.rs:79-153` (arm 101-112, closed fall-through arms 142-150). Exact.
8. Execution from frozen spec, parents `[before, source]` in order, self-verify parents/message-bytes/tree/author/committer, frozen Commit can never publish as plain ff (`kind==FastForward && prepared==FastForward` guard) — `merge_prepared.rs:296-336` (parents 304-313, self-verify 315-327, message-bytes 318, ff guard 251). Exact.
9. Restart adoption comparing 2 parents, message bytes, tree, both signatures — `merge_prepared.rs:39-69`, consumed via `status/pending.rs:117-152`. Exact.
10. v1 lifecycle mode-aware preparation from `record.mode` (`prepared_mode: NoFf → ForceMergeCommit`), ff-able+forced → effective TrueMerge, proof-issued persistence, executes only the decoded frozen action — `v1_lifecycle/authority/observe/forward.rs:85-116 (proof), 118-218 (prepare), 188-189, 198-204, 230-236`; `v1_lifecycle/forward/execute.rs:8-66` (verify at 14, decode at 25). Exact.
11. `FfOnly`+TrueMerge rejected — `forward.rs:220-228`. Exact.
12. Reconciliation NotStarted/Completed/ExpectedConflict/Ambiguous from the frozen action only; `TrueMerge → Merged` — `status/pending.rs:26-237`; `forward.rs:274-371` (mapping 357-362). Exact.
13. Abandonment only via bound exact NotStarted consumed by abort/preserve entry, illegal after preservation — `v1_lifecycle/transition/mod.rs:153-160`, `transition/reverse_entry.rs:82-88`, `transition/effect.rs:38-39`; contract `GwzM5-8I2RecordContract.md:354-363`. Exact.
14. Exactly two end-to-end no-ff tests — `v1_lifecycle/tests/forward.rs:81-100` (two parents, parent order asserted at 97-99) and `:103-139` (crash then exact adoption). Exact; grep confirms no other no-ff test in `v1_lifecycle/tests/`.
15. Unreachability idiom: `#[cfg(test)] mod v1_lifecycle;` + sentinel `merge/mod.rs:23-26`; dead-code allowance naming A1 `v1_lifecycle/mod.rs:1-5`; `PRODUCTION_R3` + `unreachable!` v1 arm `record_wire/decode.rs:82-87`. Exact.
16. v1 wire acceptance of `mode: no_ff` — only the matrix constrains it; grep of `model/v1/validate/` confirms `action.rs:67` is the sole NoFf site. Exact.

**One row is accurate only on a test-gated path and the design's §3.5 overstates it — see F-1.** Row "Open v0 `mode: no_ff` is rejected before adaptation/migration (`UnsupportedLegacyMode`)": the gate exists at `record_wire/open_v0/adapter.rs:88-96`, but the entire `open_v0` module is `#[cfg(test)]` (`record_wire/mod.rs:13-14`), and `UnsupportedLegacyMode` is raised **nowhere else in the crate** (grep: only `adapter.rs:90` plus protocol number mapping). The §2.1 row's own wording ("before adaptation/migration") is technically truthful; §3.5 item 1's expansion of it is not (F-1).

## 2. WIRE CLAIM (§3)

**Zero-new-fields: CONFIRMED.** Sweep of `NoFf|no_ff` across src finds no no-ff wire beyond what §3 enumerates: request enum `protocol/generated.rs:274` (NoFf=2, rejected at validate), the shared record `mode` field, the existing action vocabulary, protocol error 47 (`GwzM5-8I2ProtocolContract.md:17`) and wave string `A1 (v1 integration/acceptance/no-ff)` (`:44-45`). No response mode projection (`response.rs` grep clean). No journal variant carries mode (`GwzM5-8I2ActionJournalContract.md:44-62` verified; `ResetIntegrated` observation at `:222-231` anchors on recorded result/`before_commit`, mode-blind, exactly as §5.3 states). Parents are not serialized separately (no such fields on `PendingMergeAction`).

**M5b-freezes vs A1-activates table: verified against every cited gate.** `validate.rs:12-14` and the precedence shim `runtime/dispatch.rs:115-120` (message validation deliberately skipped when mode==NoFf so the phase error wins) — both exact. `PRODUCTION_R3` v1 rejection is a **typed** error carrying `required_wave: A1` (`record_wire/header.rs:77,155`), so tripwire T-2 is real; the `unreachable!` at `decode.rs:86` guards only the impossible dispatch. Migration/open-v1/unknown-fields all `cfg(test)` (`record_wire/mod.rs:13-27,43-65`). Version-selection function: grep confirms **no `active_writer_floor` symbol in the tree** — D-list and Q2 premise correct. v1 `Serialize` only under `cfg_attr(test, …)` (`model/v1/record.rs:19`).

**M5b-W1 document-only claim: holds, with two accuracy corrections owed (F-4, F-5).** Timestamps: already wire-frozen (`model/v0.rs:131-139`), captured once (`merge_support.rs:296-317`), reused at execution (`signature_from_prepared`, `:319-344`), compared on adoption (`:346-354`) — clause 1 changes no behavior. No-GPG: both integration-commit creators are libgit2 `repo.commit(...)` — the two-parent path `merge_prepared.rs:304-313` and the resolution path `merge_recovery.rs:424-433` — neither can emit a `gpgsig` header, so clause 2 is document-only for §7's scope. But the design's supporting sentence "no gpg/signing call sites in gwz-core/src" is false as written: the porcelain fallbacks at `repository.rs:293` (ordinary commit) and `refs.rs:171`/`contract.rs:865-866` (tags) deliberately honor hooks/signing config for non-merge objects (F-4). An `extensions`-carried `gpgsig`-like unknown field is inert (nothing consumes it; the write path cannot emit it) — "cannot carry a GPG field silently" holds in the semantic sense.

## 3. DETERMINISM TABLE (§4)

All seven inputs verified at both capture and execution; **no execution-time re-derivation found**. Parents: `expected_before`/`source_commit` params (`merge_prepared.rs:301,304-313`) come from the participant row, which `decode_for_participant` forces equal to the frozen action (`integration.rs:175-191`, `IntegrationIntent` fields at `:17-52`) — likewise the message (`row.commit_message`, NUL-re-checked at `merge_prepared.rs:228-233`, byte-equality self-verified at `:318`). Tree and both signatures come from the spec (`:299-303`); the wall clock never re-enters (`signature_from_prepared` constructs from frozen seconds/offset); libgit2 `commit()` sets no encoding/extra headers; the reflog committer (`:331`) and `checkout_tree` (`:328-329`) do not enter commit bytes. Capture points (`merge_signatures` `merge_support.rs:278-294`, fallback `:271-276`, attribution materialization `:356-386`, `model/mod.rs:338-377`) all preparation-time. §4.5 tree-lifetime claims exact (`write_tree_to` at prep `merge_prepared.rs:186`; pruned tree → typed mismatch `merge_support.rs:124-126`). §4.3 fixture precedent exact (`g02.rs` `pin_fixture_autocrlf`/`pin_fixture_identity` at ~832-856).

Two adoption-path edges the table does not state (both fail-closed or benign; P3, F-6): (a) adoption/completion matching is **field-wise, not OID/byte-wise** — a hand-crafted twin commit with identical parents/tree/message/identities plus an extra header (e.g. `gpgsig`) passes `commit_matches_prepared_merge` and is adopted Completed with a different OID than the §4.1 pure function yields; the "byte-identical" claim is true only of gwz-*executed* commits. (b) A forged spec whose signature name/email carry libgit2-"crud" (leading/trailing spaces — not caught by `action.rs:103-109`, which forbids only `\0\n\r`) is trimmed by `git2::Signature::new`, so execution succeeds but the created commit can never satisfy `signature_matches_prepared` afterward — permanently Ambiguous; wrong-evidence-proof, availability-loss only.

## 4. UNREACHABILITY (§6) — the adversarial hunt

Tripwires: T-1 exists (cited test verified). T-2 real and typed (`header.rs:77,155`). T-4 verified feasible — full-crate grep: `ForceMergeCommit` construction/callers are `v1_lifecycle/authority/observe/forward.rs:198,232` (cfg-test module), `git/tests/g12.rs:217` (test), plus declaration and the default-trait **rejection** (`contract.rs:269-280`, ForceMergeCommit arm 277-279); production start/continue funnel through AllowFastForward (`start/execution.rs:151-167`, `continue_op/execution.rs:117`). T-5 matches `GwzM5-8I2CompatibilityContract.md:266-273`. T-3 as worded would fail against the existing corpus (F-7): g23 **deliberately serializes** `mode: no_ff` v0 records as negative fixtures (`workspace_ops/tests/g23/compatibility_v0.rs:94-97`, `compatibility_v0_edges.rs:38,68,164-180`, `atomic_upgrade_v0.rs:79-85`).

**The constructed call path the design's argument misses (F-1, P1).** The three gates protect the request surface, the v1 record surface, and the ForceMergeCommit *preparation* arm. They do not protect the **v0 forged-record lane into the two-parent execution machinery**, which is production-compiled and production-reachable today:

1. Forge an open v0 record (mode field irrelevant — `no_ff` or even omitted): participant in a retry-eligible state, `pending_action` = kind `true_merge` / expected `commit` / `commit_spec.tree_oid` = the source commit's tree, over a genuinely **fast-forwardable** `(before_commit, source_commit)` pair.
2. Production load: `store/mod.rs:244-247` → `decode_production_v0` (NoFf is a valid enum variant) → `validate_record` (`store/mod.rs:197-214`) checks merge-id/schema/filename **only**. No mode gate, no (mode,kind) matrix: `validate_v0_structure` is called solely from the cfg(test) adapter (`adapter.rs:97`), and nothing on the production resume path consumes `record.mode` at all (full-crate grep; mode is read only at plan time, `plan.rs:211-241`).
3. `gwz merge resume`: continue preflight takes the **durable** branch (`continue_op/execution.rs:42-58`), maps `TrueMergeCommit(spec)` straight to `GitPreparedMerge::Commit` (`:170-173`) without re-preparation; reconciliation classifies NotStarted through the `(FastForward, Commit)` arm (`merge_support.rs:101-112` — the arm that exists *only* for no-ff semantics, since analysis of a fixed commit pair is immutable and no legitimate v0 writer can produce this shape); execution then creates the two-parent merge commit over the ff-able pair (`merge_prepared.rs:296-336`).

Result: **no-ff semantics — a two-parent commit where fast-forward was possible — executes in the production v0 binary today**, in direct contradiction of the amended contracts the design freezes against: `GwzM5-8I2CompatibilityContract.md:163-165` ("A v0 `mode: no_ff` open row remains `UnsupportedLegacyMode` **before resume**/mutation") and `GwzM5-8I2RecordContract.md:343-344` ("rejected before adaptation, migration, record rewrite, or Git mutation"). The only implementation and the only tests of that promise live on the cfg(test) R3-adaptation path (`adapter.rs:88-96`; `assert_unsupported_no_ff` calls `adapt_open_v0_for_r3_tests`). The design's §3.5 item 1 cites the test-gated gate as if it discharged the contract for hand-forged rows generally, its §3.2 header says everything M5b freezes is "all production-unreachable" while the *consumption* half of item 2 is reachable as above, and no tripwire T-1..T-5 covers this lane. By the design's own exit criterion 4 ("any additional production-reachable no-ff [surface] found = NO-GO"), this blocks.

## 5. PROOF PLAN (§7)

Coverage against Refactor bullets 2-4 and the record-contract exit bullet (`GwzM5-8I2RecordContract.md:420-421`): P-DET discharges "deterministic two-parent commit" in its strongest (OID-pure) form; P-FWD covers up-to-date, conflicting-divergent, crash-before-execution, and the normal/ff-only sweep; P-REV covers abandonment both ways, refusal-after-creation, rollback, preservation; P-V0 re-runs the M4 corpora with a diff-guard. Suite names follow existing harness conventions; §5.2-compliant.

**Missing rows:** (a) **P2 (F-2):** the frozen matrix cell "no-ff, fast-forwardable **or clean true merge** → true-merge/commit" has no suite for its clean-true-merge half — both existing no-ff tests use ff-able fixtures (`tests/forward.rs:82,104` — `Kind::FastForward`), and no NEW row proves divergent-clean under `no_ff` end-to-end (commit equals normal-mode's byte-for-byte; tree = merge-index tree, not source tree). The exit bullet demands the *complete* matrix. (b) **Tied to F-1:** no row pins production-v0 behavior for a forged open `no_ff`/forged two-parent-action record (whatever disposition F-1's remediation chooses, it needs a named suite/tripwire T-6). (c) P3: no negative row for a forged non-canonical signature spec (the crud-trim edge, F-6b).

## 6. CONSISTENCY

- **Amended I2 contracts:** consistent except F-1 (above) and a P3 ordering slip — §3.5 says "structurally validated, then `UnsupportedLegacyMode`"; the adapter checks mode at `adapter.rs:88` **before** structural validation at `:97` (envelope-validated only).
- **ExactEvidencePlatformAmendment:** §5.4's claims verified against the amendment — forward no-ff edges enumerated untouched (`:94-95`), unrewritten-smudged-files open exposure with forward-integration materialization named (`:99-112`, State F1 = Code P2), Clause B′ reach explicitly including `merge_prepared.rs:335` (`:257-263`), Clause A recovery rows (`:87-88`). Consistent; M5b adds no rewrite edge (the two-parent `checkout_tree` at `merge_prepared.rs:328-329` is the already-enumerated forward class). **However (F-3, P3):** §8.2 still calls the foreign-filter policy and real-Windows satisfiability "two OPEN DECISIONS … tracked review debts" — the checkpoint (same date, commit `0298edb`) records **A1 decisions ADOPTED 2026-08-16**: D1 = Option B creation-time filter neutralization plus an un-pinned CRLF matrix sentinel, D2 = A′ refined refusal, release-gated. §8.2 and Q7's premise must be refreshed (D1's sentinel partially answers Q7 at class level).
- **R4b-G dependency (§8.2):** sound. Resume order confirmed (`CurrentProgramCheckpoint.md:315-316`; `GwzMergeCheckpoint-v0.10.5.md:127-130`); ~3.5k unaccepted `v1_lifecycle` production lines confirmed (`GwzM5-8ProgressReviewF5.md:58`); pipeline-lane rule confirmed (`GwzProcessOptimization.md:125+` §4.4); freeze/review/W1/suite-drafting genuinely R4b-G-independent while IMPL merge waits — the moving-target and call-graph-gate arguments hold. P3: the design cites the resume order at `CurrentProgramCheckpoint.md:294-295`; it is at ~315-316.
- **Ledger:** `GwzM5-8ChangeBudget.md` `###` sections end at R4b-P0.2 — D6 confirmed. PRF5 overrun citation (`:427-431`) and §9-item-5 A1 pinning (`:453-454`) confirmed.

## 7. LANE POSITIONS (Q1-Q8)

Q1 file W1: **sound** (wire already carries the timestamps; the Refactor's "signatures" wording is genuinely ambiguous) — fold F-4/F-6a scope corrections into it. Q2 version-selection stays in A1: **sound** (symbol absent; it is activation machinery; moving it in would spend the last cheap moment on non-no-ff work). Q3 record-level mode: **sound** (matches `forward.rs:188-189`; wire-neutral to revisit). Q4 from-freeze determinism: **sound** (code does exactly this; cross-op reproducibility would change frozen wire meaning). Q5 seam-parameterized suites: **sound, endorse** (A1's Refactor 2299-2300 reuse is real). Q6 IMPL tier: defensible either way under §4.2; given F-1 arose at this freeze and M5b sits directly under the A1 cliff, I recommend recording **mandated dual** for M5b-IMPL. Q7: with D1 adopted, class-level coverage plus the D1 CRLF sentinel likely suffices **if** the classification ledger names the no-ff materialization class explicitly; otherwise add the sentinel — either disposition fine once §5.4 is updated (F-3). Q8 ceilings: **ratify the 0-production-line ceiling**, but only after F-1's disposition is written down — if the owner decides the "before resume" contract promise must be discharged pre-A1, that gate is production lines and must be scoped to a named package (M5b ceiling-exception or a separate slice), not quietly absorbed.

## 8. FINDINGS

- **F-1 (P1, blocks freeze).** Production v0 resume executes forged two-parent-over-fast-forwardable actions; the amended contracts promise `UnsupportedLegacyMode` "before resume/mutation" (`GwzM5-8I2CompatibilityContract.md:163-165`, `GwzM5-8I2RecordContract.md:343-344`); the only gate is cfg(test) (`adapter.rs:88-96` via `record_wire/mod.rs:13-14`); no production mode consumer after plan time; `continue_op/execution.rs:42-58,170-173` + `merge_support.rs:101-112` + `merge_prepared.rs:296-336` complete the path. §3.5 item 1 asserts the opposite as a tree fact; §3.2 labels the frozen set "all production-unreachable"; no tripwire covers the lane. Remediation is document-plus-plan: state the truth in §3.5/§6, add tripwire T-6 with an owned disposition (production gate now with a ceiling exception, or contract-text amendment re-scoping the promise to A1 with the forged-row fixture pinned to current behavior), and reconcile Q8.
- **F-2 (P2, blocks freeze).** §7 lacks the clean-true-merge half of the frozen no-ff matrix cell (`GwzM5-8I2RecordContract.md:331`); both existing no-ff suites are ff-able-based; the record-contract exit bullet demands the complete matrix. Add the row (e.g. `no_ff_clean_true_merge_matches_normal_mode_bytes`).
- **F-3 (P3).** §8.2/Q7 stale vs the checkpoint's adopted A1 decisions D1/D2 (2026-08-16).
- **F-4 (P3).** §4.4's "no signing call sites in gwz-core/src" is overbroad (`repository.rs:293`, `refs.rs:171`, `contract.rs:865-866` honor signing for non-merge objects); scope the sentence and W1's rationale to the merge-integration commit creators.
- **F-5 (P3).** §3.5 item 1 ordering: mode rejection precedes structural validation (`adapter.rs:88` vs `:97`).
- **F-6 (P3).** State in §4/§5 that adoption/completion matching is field-wise, not OID-wise (signed-twin adoption possible; byte-identity holds for gwz-executed commits only), and that non-canonical (space-padded) forged signature specs execute-then-never-reconcile (fail-closed); consider a validator trim rule or a negative suite row.
- **F-7 (P3).** T-3 as worded fails against g23's deliberate `no_ff` negative fixtures; reword to writer-produced/positive fixtures.
- **F-8 (P3).** Citation corrections: resume order is at `CurrentProgramCheckpoint.md:~315-316` (not 294-295); gwz-core HEAD is `43c37bc` (design cites `f36d20d`; delta workflow-only — restate or re-pin at freeze).

## 9. VERDICT

**NO-GO** for freezing M5b-IF on this draft. The inventory, wire posture, determinism semantics, reconciliation authority rule, v1-side unreachability gates, and the R4b-G dependency statement are otherwise verified accurate at the cited lines — the machinery claims all held under adversarial checking, and no v1-side production route to `ForceMergeCommit` or the v1 writer exists. The block is F-1 (a contract-vs-production divergence the freeze text asserts away, with its Q8 ceiling interaction undecided) plus F-2 (a genuine matrix hole in the proof plan). Both are remediable in the document and proof plan without touching code; I expect a focused re-review to reach GO.

Tuple: design doc **uncommitted** (untracked `dev-docs/GwzM5-8M5bNoFfDesign.md`, gwz-dev at `0298edb`); gwz-core HEAD `43c37bcff338daf95f053eedbc7467e6c9368eff`; date **2026-08-16**.

---

# APPENDED ROUND-2 RE-VERDICT (against `cdb7ec1`): NO-GO on N-1 alone — all F-1..F-8 REMEDIATED (F-1 exactly and without softening); new findings N-1 (P2: the T-4 `#[cfg(not(test))]` compile-shim contradicts the ratified §8.3 ceiling and has no coherent zero-production form), N-2/N-3 (P3 phrase corrections). Pre-commitment: GO on a revision resolving N-1 as specified. (Full round-2 text in the lane record; summarized here at filing.)

---

# APPENDED ROUND-3 ACK (focused check of the four round-2 edits, `cdb7ec1 → 66117b0`)

Structural confirmation complete: `VerifiedParticipantOutcome` is issued **only** from `observe_participant_action`'s three reconciliation arms (`forward.rs:299/344/363 → :378`); `execute.rs::participant` returns `()`, so there is no direct-outcome-write from an execution result.

**N-1 (T-4) — RESOLVED, matches my specification exactly.** Reverted to the structural-scan variant; my reasoning recorded verbatim (production-compiled shim contradicts the ratified §8.3 ceiling; no coherent zero-production form because production legitimately matches on the variant at `merge_prepared.rs:140-195` / `contract.rs:269-280`; poisoned variant = compile break, not tripwire). The State-concern preservation note (scan executes in CI via the checker lane) is ceiling-compatible — a scan in the test/checker lane is zero production lines — and machine-checked as required.

**N-2 (§2 clause) — ACCURATE.** The false "no cited semantics" sentence is gone; the replacement is exactly my overlap statement: `merge_recovery.rs` cited at `:424-433` (verified against the committed blob), uncommitted hunks at `:6/:149+/:224+` inside `abort_merge`/`set_branch_target_checked`, overlapping no cited range; committed-blob basis and re-cut-at-acceptance retained.

**N-3 (D3 bullet) — ACCURATE.** Train state now current: `e9396a9` confirmed in the log between `cdb7ec1` and `66117b0`; re-verdicts pending at ACK time; "wire shape affirmed on both axes" matches the filed reviews verbatim. The folded State-P4 binds-on-the-property clause is consistent with the §7 P-REV order-independence instruction and weakens no M5b position.

**Fourth edit (§4.6 State-P3-new scoping clause) — one new inexactness, N-4 (P3, non-blocking).** The added parenthetical "(an uninterrupted run completes its outcome write normally; the permanent Ambiguity materializes when restart-based reconciliation … intervenes)" is code-false. In the v1 lifecycle there is no outcome-write path from the execution result: `forward/execute.rs::participant` returns `ModelResult<()>`, and the only forward issuers of `VerifiedParticipantOutcome` are `observe_participant_action`'s reconciliation arms (`forward.rs:299/344/363`), whose Completed classification for `TrueMergeCommit` runs `commit_matches_prepared_merge` → `signature_matches_prepared` (`pending.rs:134-151`) — comparing the live, libgit2-trimmed identity against the frozen space-padded wire values, which fails. So for a non-canonical forged spec, **even an uninterrupted run stops Ambiguous at the first post-execution observation**; that is the R4b-X doctrine working as designed. The pre-round-2 wording was correct; the fail direction, the "availability-only" framing, and the §7 suite row all remain right — the suite will adjudicate this mechanically at IMPL. Fix is one clause; flagged to the State axis as a cross-axis disagreement to reconcile in the next docs pass.

## VERDICT: **GO.**

N-1 resolved as specified; N-2/N-3 folded accurately. The single new item N-4 is P3 — non-blocking — recorded for the next documentation pass plus State-axis reconciliation. Per my round-2 pre-commitment, this axis is GO for freezing M5b-IF on the design at `66117b0`, with M5b settled acceptance bound as frozen (T-6: gate package landed and its two suites green; tuple re-cut against a clean committed gwz-core tree at acceptance).

Tuple: design doc **committed** at gwz-dev `66117b0`; gwz-core HEAD `43c37bcff338daf95f053eedbc7467e6c9368eff`; date **2026-08-16**.
